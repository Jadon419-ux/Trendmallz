services:
  - type: web
    name: trendmallz
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: SHIPBUBBLE_API_KEY
        sync: false
